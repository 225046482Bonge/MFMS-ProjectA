/* stubs.c - TEMPORARY placeholders (Student 6)
 * When a teammate pushes a real module, delete that stub function
 * from this file, and remove stubs.c from the Makefile once it is empty. */
#include <stdio.h>
#include "mfms.h"
#include "validation.h"

void employeeMenu() { printf("\n[Employee module not ready yet]\n"); pauseScreen(); }
void budgetMenu()   { printf("\n[Budget module not ready yet]\n");   pauseScreen(); }
void supplierMenu() { printf("\n[Supplier module not ready yet]\n"); pauseScreen(); }
void assetMenu()    { printf("\n[Asset module not ready yet]\n");    pauseScreen(); }
void reportsMenu()  { printf("\n[Reports module not ready yet]\n");  pauseScreen(); }
